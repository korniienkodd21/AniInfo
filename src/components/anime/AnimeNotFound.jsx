import Heading from "../../ui/Heading";

const AnimeNotFound = () => {
   return (
      <Heading
         className="text-raspberry-pink"
         text="Sorry, this anime was not found. Please try again."
         level={2}
      />
   );
};

export default AnimeNotFound;
